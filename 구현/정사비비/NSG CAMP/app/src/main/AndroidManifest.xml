<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nsg22">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity_campbookmark"></activity>
        <activity android:name=".activity_campupload" />
        <activity android:name=".activity_wt_review" />
        <activity android:name=".activity_mycamping" />
        <activity android:name=".activity_reviewlists" />
        <activity android:name="com.activity_reviewlists" />
        <activity android:name=".activity_review" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>